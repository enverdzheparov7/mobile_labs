<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animationapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="34" />

    <permission
        android:name="com.example.animationapp.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.animationapp.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="true"
        android:icon="@drawable/circle_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/circle_background"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar" >

        <!-- ГЛАВНЫЙ ЭКРАН — меню всех заданий -->
        <activity
            android:name="com.example.animationapp.MenuActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Часть II — базовые анимации -->
        <activity android:name="com.example.animationapp.MainActivity" />

        <!-- Часть III — Lottie -->
        <activity android:name="com.example.animationapp.LottieActivity" />

        <!-- Задание 1 — Activity Transition -->
        <activity android:name="com.example.animationapp.SecondActivity" />

        <!-- Задание 2 — Анимированная кнопка -->
        <activity android:name="com.example.animationapp.AnimatedButtonActivity" />

        <!-- Задание 3 — RecyclerView -->
        <activity android:name="com.example.animationapp.RecyclerActivity" />

        <!-- Задание 4 — Progress Indicator -->
        <activity android:name="com.example.animationapp.ProgressActivity" />

        <!-- Задание 5 — Анимированная иконка -->
        <activity android:name="com.example.animationapp.AnimatedIconActivity" />

        <!-- Задание 6 — Fragment Transition -->
        <activity android:name="com.example.animationapp.FragmentTransitionActivity" />

        <!-- Задание 7 — Анимированная карточка -->
        <activity android:name="com.example.animationapp.AnimatedCardActivity" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.example.animationapp.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>