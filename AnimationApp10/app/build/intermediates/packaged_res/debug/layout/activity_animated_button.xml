<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Задание 2: Анимированная кнопка"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="32dp" />

    <!-- Анимированная кнопка: scale + цвет при нажатии -->
    <Button
        android:id="@+id/animatedButton"
        android:layout_width="200dp"
        android:layout_height="56dp"
        android:text="Нажми меня!"
        android:textColor="#FFFFFF"
        android:background="@drawable/btn_animated_background"
        android:stateListAnimator="@null" />

    <TextView
        android:id="@+id/tvClickCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Нажатий: 0"
        android:textSize="16sp" />

</LinearLayout>
